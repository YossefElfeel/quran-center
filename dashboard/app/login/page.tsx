export default function LoginPage() {
  return (
    <main className="mx-auto flex max-w-md flex-1 flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-2xl font-bold text-primary">تسجيل دخول السوبر أدمن</h1>
      <p className="text-foreground/70">
        شاشة الدخول هتتبني في Phase D0/D1 (Supabase Auth + بوابة super_admin).
      </p>
    </main>
  );
}
