{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && cd web && npm install",
  "buildCommand": "cd web && npm run build && cd .. && ln -sfn web/.next .next",
  "outputDirectory": "web/.next"
}
