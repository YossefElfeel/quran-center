/// اختيار مستوى في القوائم المنسدلة (id + label للعرض: "المنهج — المستوى").
class LevelOption {
  const LevelOption({required this.id, required this.label});

  final String id;
  final String label;
}
